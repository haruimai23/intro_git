ブロック崩し 1 テスト仕様書

対象: index_game_breakout1.html

1. 概要

HTML/JavaScriptで実装したブロック崩し(ブレイクアウト)ゲームの初版(1版)の動作を検証するためのテスト仕様書。パドル操作・ボール反射・ブロック破壊・残機管理・スコア/ハイスコア表示・一時停止・クリア/ゲームオーバー判定を実装している。

2. テスト環境

モダンブラウザ(Chrome / Edge / Firefox 最新版)
BASE_URL: http://160.16.230.177
http://160.16.230.177/index_game_breakout1.html にアクセスして実施

3. テストケース

3.1 初期表示

No 1-1 初期画面表示 index_game_breakout1.html を開く ゲームボード(#board)、スコア欄、ハイスコア欄、残機欄、スタートボタン、一時停止ボタン、ハイスコアリセットボタン、操作方法の説明が表示される
No 1-2 初期スコア 画面を開いた直後の状態を確認 スコア: 0、残機: 3 と表示される
No 1-3 初期ハイスコア ハイスコアを保存していない状態で画面を開く ハイスコア: 0 と表示される
No 1-4 ブロック配置 画面を開いた直後の盤面を確認 5行8列(40個)のブロックが盤面上部に配置されている
No 1-5 最上段ブロック色 画面を開いた直後の盤面最上段(1行目)のブロックの色を確認 最上段のブロックがピンク色(#ff69b4)で表示される

3.2 ゲーム開始

No 2-1 スタートボタン押下 「スタート / 再スタート」ボタンをクリック ボールが盤面中央のパドル上から上方向に移動を開始する
No 2-2 再スタート ゲームプレイ中に「スタート / 再スタート」ボタンを再度クリック 盤面(ブロック)・スコア・残機がすべて初期化される(ハイスコアは初期化されない)

3.3 操作(パドル移動・一時停止)

No 3-1 左移動 ゲーム中に「←」キーを押す パドルが左に移動する(盤面左端で停止する)
No 3-2 右移動 ゲーム中に「→」キーを押す パドルが右に移動する(盤面右端で停止する)
No 3-3 一時停止 ゲーム中に「一時停止」ボタンまたは「P」キーを押す ボールとパドルの動作が止まり、「一時停止中」のメッセージが表示される
No 3-4 一時停止解除 一時停止中に再度「一時停止」ボタンを押す ゲームが再開し、メッセージが消える

3.4 ブロック破壊とスコア

No 4-1 ブロック破壊 ボールがブロックに当たる ブロックが消え、スコアが10点加算される
No 4-2 全ブロック破壊 全てのブロックを破壊する 「クリア!」のメッセージが表示され、ゲームが停止する

3.5 残機とゲームオーバー

No 5-1 ボールを取り逃す パドルが無い位置をボールが通過して盤面下端に到達する 残機が1減る
No 5-2 残機0でゲームオーバー 残機が0になる 「ゲームオーバー」のメッセージが表示され、操作ができなくなる
No 5-3 ゲームオーバー後の再開 ゲームオーバー状態から「スタート / 再スタート」ボタンをクリック 盤面・スコア・残機が初期化され、新しいゲームが開始する

3.6 ハイスコア

No 6-1 ハイスコア更新 プレイ中にスコアがそれまでのハイスコアを上回る ハイスコア表示が現在のスコアと同じ値に更新される
No 6-2 ハイスコアの保持 ハイスコア更新後にページを再読み込みする 再読み込み後もハイスコアの値が保持されて表示される
No 6-3 ハイスコアリセット 「ハイスコアをリセット」ボタンをクリックする ハイスコア表示が0に戻り、再読み込みしても0のままである

4. 非機能要件確認

No 7-1 外部依存 外部ライブラリ・CDNへの依存がなく、index_game_breakout1.html 単体でブラウザ表示・動作が完結する
No 7-2 キー入力の既定動作抑制 矢印キー押下時にブラウザのスクロール等の既定動作が発生しない
No 7-3 ハイスコア保存先 ハイスコアはブラウザのlocalStorageにキー名 breakout1HighScore で保存される

5. テスト自動化用フック

自動テストでの状態確認・再現困難な状態(全ブロック破壊、残機0)の検証を安定させるため、window.__test オブジェクトを公開している。通常のゲームプレイ・表示には影響しない。

window.__test.getState(): score・lives・paddleX・ballX・ballY・bricksAlive・running・paused・gameOver・cleared を返す
window.__test.forceWin(): 全ブロックを破壊済みにしてクリア状態に遷移する(クリア時のUI確認用)
window.__test.forceGameOver(): 残機を0にしてゲームオーバー状態に遷移する(ゲームオーバー時のUI確認用)

6. 既知の制約

ボールの反射角度はパドルに当たった位置に応じて変化する簡易的な物理モデルであり、実機のブロック崩しゲームの厳密な挙動とは異なる場合がある。
モバイル(タッチ操作)には対応していない。
ハイスコアはブラウザ単位(同一オリジンのlocalStorage)で保存されるため、別ブラウザ・別端末・シークレットモードでは共有されない。
レベル(難易度)や複数ステージには対応していない(初版のため単一ステージのみ)。

7. 変更履歴

修正1回目: 最上段(1行目)のブロックの色を赤(#f00000)からピンク(#ff69b4)に変更。
