DBログテーブル確認画面 テスト仕様書

対象: index_DB_test99.php

1. 概要

application_logsテーブルの全レコードを取得して一覧表示するPHP画面(index_DB_test99.php)の動作を検証するためのテスト仕様書。初期表示では何も表示せず、表示ボタン押下時にのみDBへ接続してレコードを取得・表示する。

2. テスト環境

モダンブラウザ(Chrome / Edge / Firefox 最新版)
BASE_URL: http://160.16.230.177
http://160.16.230.177/index_DB_test99.php にアクセスして実施
サーバー側にPHPおよびapplication_logsテーブルを持つMySQL(test DB)が稼働していること

3. テストケース

3.1 初期表示

No 1-1 初期画面表示 index_DB_test99.php を開く 「表示」ボタンと「閉じる」ボタンが表示される
No 1-2 ログ一覧の初期状態 画面を開いた直後の状態を確認 ログ一覧テーブル(#logTable)・データなしメッセージ(#noDataMessage)・エラーメッセージ(#errorMessage)のいずれも表示されていない
No 1-3 表示ボタンの背景色 画面を開いた直後に「表示」ボタン(#showBtn)の背景色を確認する 「表示」ボタンの背景色が青(#0000ff)である

3.2 表示ボタン押下

No 2-1 レコードが存在する場合 application_logsテーブルにレコードが存在する状態で「表示」ボタンをクリック テーブル(#logTable)が表示され、テーブルの列見出しと各レコードの内容が一覧表示される
No 2-2 レコードが存在しない場合 application_logsテーブルが空の状態で「表示」ボタンをクリック 「データがありません」というメッセージ(#noDataMessage)が表示される
No 2-3 DB接続に失敗した場合 DBサーバーに接続できない状態で「表示」ボタンをクリック 「DB接続エラー」を含むメッセージ(#errorMessage)が表示される

3.3 閉じるボタン押下

No 3-1 閉じるボタン押下 「閉じる」ボタンをクリックする 画面(ウィンドウ/タブ)を閉じる処理(window.close())が呼び出される

4. 非機能要件確認

No 4-1 DB接続情報 DB接続情報(ホスト・DB名・ユーザー名・パスワード)はサーバー側のPHPコード内に保持され、画面上には表示されない
No 4-2 出力エスケープ テーブルに表示するカラム名・値はhtmlspecialcharsによりエスケープされ、HTMLとして解釈されない
No 4-3 固定クエリ application_logsテーブルの取得は固定のSELECT文で行われ、ユーザー入力を直接SQL文に埋め込まない
No 4-4 画面タイトル 画面のタイトルが「DBログテーブル確認画面」であること

5. 既知の制約

閉じるボタンはブラウザの仕様上、スクリプトから開かれていないウィンドウ・タブでは動作しない場合がある
表示されるレコードの件数・内容は実行環境のDBの状態に依存する
DB接続情報は暫定のものであり、本番運用時は別途安全な方法で管理する必要がある
